import { StyleSheet } from 'react-native';

export const calendarStyles = StyleSheet.create({
  calendar: {
    margin: 16,
    borderRadius: 12,
    elevation: 2,
    backgroundColor: '#fff',
  },
});